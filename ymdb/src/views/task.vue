<template>
    <div class="task text-left">
        <h2>Task</h2>
        <p>You should add the search component to the Home view. Use props to pass in text values for the placeholder and the label</p>
        <ol>
            <li>Import the search component in /src/main.js and register it as a globally available component</li>
            <li>Add the component to the Home view template and add attributes to pass data to the props</li>
            <li>Edit the search component so the label text and input field placeholder text is taken from the props</li>
        </ol>
    </div>
</template>